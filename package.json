{
  "name": "ewc-team-command-center",
  "version": "1.0.0",
  "private": true,
  "description": "Packaged EWC Team Command Center with behavior preserved from the original single-file page.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
