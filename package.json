{
  "name": "ewc-team-command-center",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone EWC Team Command Center repository with bundled reference data and assets.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
